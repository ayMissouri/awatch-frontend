import { en } from "./locales/en";

// `t` is the active translation dictionary
//
// import { t } from "@/i18n";
// <h1>{t.home.hero.title}</h1>
export const t = en;

export type Translations = typeof en;
